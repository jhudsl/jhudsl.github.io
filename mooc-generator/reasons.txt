My app will allow users to take google slides (Slides API) and convert them into videos (YT API).  The app needs Drive API access so that it can search for slide decks.  It will also have the ability to translate the words into different languages.